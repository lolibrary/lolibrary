// Package models contains all of the generic base models used by the app.
// As the app is a monorepo, every service can take advantage of common models
// that are passed around, reducing code complexity.
package models

// Model is a base model that all models inherit from.
type Model struct{}
